class Solution:
    def climbStairs(self, n: int) -> int:
        #day 15 josephineoe
        a = 1
        d = 1
        for i in range(1, 45):
            steps = a + (n-1)*d
            
        return steps
# this was actually a bit easier to solve